@model Barebone.ViewModels.Shared.MenuGroup.MenuGroupViewModel
<div data-code="main-@(this.Model.Position)">
  <div>
    @Model.Name
  </div>
  <div>
    @foreach (var menuItem in this.Model.MenuItems)
    {
      @Html.Partial("_MenuItem", menuItem)
    }
  </div>
</div>